# infra-cluster-kind

Kind-based local Kubernetes cluster  with MetalLB (LoadBalancer for local envs)

## The repo contains
- kind cluster config
- metalLB installation and address pool (layer2)
- helper notes/commands

## Goal
To simulate a cloud-like environment locally (EXTERNAL-IP for LoadBalancer Services)
